language: c
addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libx11-dev
      - libxt-dev
      - libimlib2-dev
      - libxinerama-dev
      - libjpeg-progs
build:
  - make
  - make test
compiler:
  - clang
  - gcc
